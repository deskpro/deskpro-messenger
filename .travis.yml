language: node_js
node_js:
  - '8'
cache:
  directories:
    - packages/deskpro-messenger/node_modules
    - packages/deskpro-messenger-setup/node_modules
install:
  - yarn
  - yarn bootstrap
before_deploy:
  - yarn lerna run build-storybook
env:
  REACT_APP_API_SERVICE: 'polling'
  REACT_APP_API_BASE: 'http://site35715.deskprodemo.com'
deploy:
  - provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    keep-history: true
    local-dir: packages/deskpro-messenger/storybook-static
    on:
      branch: master
