language: node_js
node_js:
  - '8'
cache:
  directories:
    - packages/deskpro-messenger/node_modules
    - packages/deskpro-messenger-setup/node_modules
install:
  - yarn
  - yarn bootstrap
before_deploy:
  - yarn lerna run build-storybook
env:
  REACT_APP_API_SERVICE: 'polling'
  REACT_APP_API_BASE: 'http://site36000.deskprodemo.com'
  REACT_APP_FROALA_KEY: 'MC1D2D1G2lG4J4A14A7D3D6F6C2C3F3gSXSE1LHAFJVCXCLS=='
deploy:
  - provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    keep-history: true
    local-dir: packages/deskpro-messenger/storybook-static
    on:
      branch: master
