{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.3.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lerna": "lerna",
    "test": "cd packages/deskpro-messenger && yarn test",
    "publish-npm": "lerna publish --force-publish --no-git-tag-version --no-push --yes"
  }
}
